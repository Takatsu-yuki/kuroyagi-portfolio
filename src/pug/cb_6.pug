extends _template/_base
include work/detail_cb

block vars
  - lev1ID = "work";
  - lev = 2;
  - titleChild = "作品一覧";

  block links
    link(href=`${pathToCSS}detail_cb.css${hash}`, rel="stylesheet", type="text/css")

block main
  +detail(6,"CHILDRENS’BOOK | お仕事","甲虫パラダイス！" , "2024.05", "3日","ぬりえっこ！2024年の7月号。山をテーマに制作しました。")

block scripts
  script(src=`${pathToJS}main.js`)
