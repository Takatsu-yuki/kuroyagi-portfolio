extends _template/_base
include work/detail_cb

block vars
  - lev1ID = "works";
  - lev = 2;
  - titleChild = "WORKS";

  block links
    link(href=`${pathToCSS}detail_cb.css${hash}`, rel="stylesheet", type="text/css")

block main
  +detail(1,"CHILDREN’S BOOK | お仕事","チョコザップ様PR漫画" , "2024.04", "1日","チョコザップPRでPR漫画を描かせていただきました。")

block scripts
  script(src=`${pathToJS}main.js`)
