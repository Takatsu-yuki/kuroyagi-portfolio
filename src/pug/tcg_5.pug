extends _template/_base
include work/detail_bace

block vars
  - lev1ID = "top";
  - lev = 2;
  - titleChild = "WORK";

  block links
    link(href=`${pathToCSS}detail_bace.css${hash}`, rel="stylesheet", type="text/css")

block main
  +detail(5,"GAME&TCG | オリジナル","杜の精霊" , "2024.08", "5日","ケモノ系モンスターを制作しました。")

block scripts
  script(src=`${pathToJS}main.js`)
